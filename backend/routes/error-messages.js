const errMsg = {
	gen: {
		success: false,
		message: 'A server error occurred, please try again.'
	},
	auth: {
		success: false,
		message: 'Could not authenticate user. Operation not completed.'
	}
};

// export default errMsg;
module.exports = errMsg;